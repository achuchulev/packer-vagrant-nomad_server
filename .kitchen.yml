---
driver:
  name: vagrant

provisioner:
  name: shell

platforms:
  - name: nomadserver
    driver:
      box: nomadserver
      provider: virtualbox

verifier:
  name: inspec

suites:
  - name: default
    verifier:
      inspec_tests:
        - test/integration/default
